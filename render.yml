version: 1
services:
  - type: web
    name: discord-bot-hendra-coders
    env: node
    plan: starter
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      DISCORD_TOKEN: "" # Ini placeholder, token sudah diatur di Render
